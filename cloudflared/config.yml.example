# Copy this file to cloudflared/config.yml and set your tunnel ID.
# In Cloudflare Zero Trust, route your public hostname to:
# http://uptime-kuma:3001

tunnel: your-tunnel-id-here
protocol: http2

originRequest:
  connectTimeout: 30s
  noTLSVerify: true
